# FunGuy's project log

This is the continuing record of implementation, findings, decisions and validation. Read the latest entries before beginning work. Keep dated entries in chronological order; append corrections or superseding decisions rather than rewriting historical results.

## Entry format

Use a concise entry for each coherent work batch. Related changes may be grouped, but every substantive change or finding must be recorded.

- **Date / subject:** When and what the work concerns.
- **Status:** Implemented, observed, proposed, blocked or superseded; distinguish mixed statuses explicitly.
- **Affected files/systems:** Paths or components involved.
- **Changes/findings and rationale:** What changed or was learned, with the reason and supporting evidence.
- **Production-path impact:** Architectural alignment, milestone progress, dependencies, or a justified deviation and migration/replacement plan.
- **Validation:** Checks actually performed, their results, and relevant checks not performed.
- **Remaining work:** Unresolved issues, follow-up tasks and decisions needed; use “None” when appropriate.

## 2026-09-14 — Initial repository review and production plan

- **Status:** Review completed; production architecture and milestone scope proposed. Runtime stage-loading defect confirmed.
- **Affected files/systems:** Repository baseline `af63cdc`; `PRODUCTION_PATH.md`; `AUDIT_VALIDATION.md`; Unity gameplay code, content, scenes, configuration and stat-sheet design.
- **Changes/findings and rationale:** Added a production roadmap and dated audit. Identified placeholder content, missing formation/progression systems, combat and UI lifecycle risks, unrestricted local economy operations and contradictory design formulas. The plan recommends progressive development of an Android-first PvE game with shared C# rules and a later authoritative online economy.
- **Production-path impact:** Establishes the baseline and milestone sequence. Stabilization and canonical rules must precede broad content expansion. Detailed evidence and proposed decisions remain in the two audit documents.
- **Validation:** Unity 6000.3.2f1 compiled the project; five EditMode tests passed and one failed. `StageWaves_ReferenceExistingEnemies` reported that `s_1_1.waves` was null. Independent JSON syntax/reference checks passed. No new Android build, device test or PlayMode navigation test was performed. Four performance-test resource files removed by the test runner were restored unchanged.
- **Remaining work:** Execute the first implementation batch in `PRODUCTION_PATH.md`, starting with the unsupported nested-wave serialization. Resolve design ambiguities before treating proposed rule choices as canonical.

## 2026-09-14 — Production architecture and continuous documentation requirements

- **Status:** User-directed workflow requirements documented and active. No gameplay implementation changed.
- **Affected files/systems:** Added root `AGENTS.md` and `PROJECT_LOG.md`; updated `PRODUCTION_PATH.md` with standing execution requirements.
- **Changes/findings and rationale:** The user requested that production architecture be implemented throughout development to avoid unnecessary backtracking, and that every change, finding and related decision be documented. Added repository-wide instructions to establish production boundaries as each system is built, keep local adapters compatible with intended application contracts, consider downstream dependencies and explain necessary temporary solutions or backtracking. Made documentation a task completion requirement, including investigations, test results and blockers.
- **Production-path impact:** All milestones now explicitly include progressive architectural implementation and synchronized documentation. Later feature scope remains deferred; the requirements do not mandate speculative infrastructure. Existing audit results remain historical evidence rather than a current validation claim.
- **Validation:** Reviewed the existing production plan and audit notes; checked documentation consistency and `git diff --check`. No runtime tests were required or run for this documentation-only change.
- **Remaining work:** Apply these requirements to the next implementation batch and append its actual findings and validation here.

## 2026-09-14 — Milestone 0 stabilization implementation

- **Status:** Implementation in progress; validation results will be recorded below. User authorized starting the build order and image generation if needed; this batch needs no new artwork.
- **Affected files/systems:** Stage schema, catalog validation, shared combat conversion, battle statuses, application services, save schema, tutorial/reveal initialization, debug UI and build tooling.
- **Changes/findings and rationale:** Migrated nested waves to schemaVersion 1 wave objects and added explicit campaign ordering. Introduced a Unity-independent content validator and shared `CombatUnitFactory`. Enemy growth now uses explicit data matching the former smoke-test values (HP +10, ATK/DEF +2, SPD/POT +1 per level); these are prototype values, not approved final balance. Fixed dead actors acting after DoT, Silence expiring before actions, multiplicative status stacking and missing Thorns reflection. Added explicit tutorial/reveal initialization and a persistent manager with per-scene overlays. Repeated binder initialization now replaces its own listeners.
- **Production-path impact:** Added `IPlayerSaveStore`, `ICampaignService` and `ISummonService` with local adapters. Battle/reward and summon operations now execute outside UI controllers; ten-pulls work on detached state and commit once. The later server replaces the application adapters. Current stage rewards are first-clear only, repeated clears are practice, and account XP is stored separately until a level curve is approved. Save schema 3 preserves legacy balances and level, starts campaign-clear tracking and prevents known tutorial reward reclaims. Runtime-generated UI remains temporary until the prefab/presentation milestone; stabilization removes known lifecycle faults without expanding art scope.
- **Validation:** Added focused regression coverage and `tools/Invoke-Unity.ps1`; EditMode execution started. Actual results, PlayMode checks and Android build status remain pending.
- **Remaining work:** Resolve any validation failures, test onboarding/resume/navigation, build Android development output, and update the living guides with final verified behavior. Burn/Bleed semantics, advanced statuses, canonical stat formulas, full save recovery/security and production backend authority remain later work as identified in the plan.

### Validation follow-up: first compile

- The first modified compile failed because `order` had been added to `BannerDef` instead of `StageDef`; corrected the declaration before rerunning. No passing result is claimed for that attempt.
- Added PlayMode onboarding/resume tests and isolated PlayerPrefs recovery/migration tests. The tests preserve and restore existing save keys. The scene bootstrap now ignores non-game scenes, keeping test-runner scenes free from game initialization.
- Local Android SDK includes platforms 34, 35 and 36. No device was connected at discovery; an Android 36 `Medium_Phone` emulator configuration is available for a later device smoke check.
- Added `BUILD_GUIDE.md` with current architecture boundaries, versioned stage/save migration policies, temporary behavior and reproducible validation/build commands.

### Validation follow-up: EditMode passes

- Reran with Unity 6000.3.2f1: **19/19 EditMode tests passed**. This includes the formerly failing bundled stage-wave test, new combat regressions, shared enemy growth, invalid content rejection, summon transaction checks, first-clear/tutorial claims and save recovery/migration.
- Started the PlayMode suite next. Android packaging and device validation are not yet established by the EditMode result.

### Validation follow-up: PlayMode passes

- **2/2 PlayMode tests passed.** Fresh-account UI events completed Boot/tutorial, summon/reveal, team, battle, first-clear/tutorial rewards and completion with persisted state. Rebinding did not duplicate a paid summon. Resume at the summon step retained the used-ticket state and reattached the overlay to the persistent manager.
- Started Android development packaging. Launched the available Android 36 emulator with `-read-only -no-snapshot -no-window -no-audio` for an isolated install/input check; no existing emulator data is intentionally changed.
- Updated the in-project testing and UI wiring guides to describe the explicit initialization contracts, new coverage and development-only QA controls.

### Build preparation findings

- Created `artifacts/validation/milestone0` from current tracked/nonignored working files with `tools/New-ValidationSnapshot.ps1`. The manifest records hashes and the copy has no preexisting Unity Library cache. Clean import/test execution remains pending.
- Android compilation progressed into shader processing, including shaders from the installed inference package. This is a confirmed dependency footprint, not yet a measured attribution of final APK size; unused-package cleanup remains a separate, validated change.

### Validation tooling review

- Confirmed all newly added Unity source assets have metadata and `git diff --check` reports no whitespace errors. The generated solution additions correspond to the new Editor build automation and PlayMode test assemblies.
- Hardened the runner to require fresh test XML/build artifacts and an explicit passing test result, preventing stale files from a previous run being mistaken for new success. The current Android process started before this runner update; its actual final log/artifact timestamps must also be checked.

### Android packaging failure and retry

- The first Android attempt completed native compilation but failed during Gradle startup: it could not write the user-wide Gradle 8.13 daemon `registry.bin`. No APK was produced. The daemon directory contains old hidden cache files; the precise permission/cache cause is not yet established.
- The runner now supplies an ignored repository-local `GRADLE_USER_HOME` (`artifacts/gradle`) for Android tasks, isolating this project from user-wide daemon state without deleting or changing that state. Retrying with the compiled native cache. First-attempt log retained under `artifacts/AndroidDevelopment/first-attempt.log`.

### Android development build passes

- Repository-local Gradle cache resolved the startup failure. `AndroidDevelopment` succeeded using Unity 6000.3.2f1, IL2CPP/ARM64, SDK 36. APK: `artifacts/AndroidDevelopment/Funguy.apk`, 68,230,335 bytes; SHA-256 `1B39B1AECC397AC2593FA0C494B4EAF222D16806FA7AAEFC680368575600EC32`.
- The successful retry's BuildPipeline duration was 7m42s; the first native compile had already completed in the failed attempt. Unity's total build-report size includes other outputs and is not the APK file size.
- Started the 19-test EditMode suite in the fresh `milestone0` source snapshot. Original working-project test reports are retained in `artifacts/initial-tests`. Emulator installation/touch input remains pending.

### Fresh-environment observations

- Clean-project package registration reports `com.unity.ai.generators` as deprecated, with its functionality moved into `com.unity.ai.assistant`. Dependency cleanup should audit actual usage before removing/replacing editor packages; this batch has not changed package versions.
- The read-only Android emulator cold boot was slow and restarted its system server before the game was installed. Early install attempts were rejected because Android was still booting. These are environment observations, not evidence of a game crash.

### Android emulator validation blocked

- Android's crash buffer confirms repeated system failures before APK installation, including `DeadSystemException` in the permission controller, phone, launcher and System UI. `sys.boot_completed` never reached 1 during this attempt. Saved evidence in `artifacts/emulator-crash.log` and stopped the isolated emulator.
- Device installation/touch navigation is **not verified**. No game crash is established because installation was rejected while Android was booting. A healthy emulator or physical device is required for this remaining gate; the passing PlayMode tests invoke button events and do not replace real touch validation.

## 2026-09-14 — Stabilization batch delivery and final validation

- **Status:** First implementation batch delivered; milestone 0 remains open for Android device/touch and non-development validation. No store release or production-readiness claim.
- **Affected files/systems:** Runtime/data/application/save/tutorial/UI changes listed above; EditMode and PlayMode regression suites; Unity build/test/snapshot tooling; living roadmap/build/test/UI guides.
- **Validation:** Working-project EditMode **19/19 passed**, PlayMode **2/2 passed**, and fresh-project EditMode **19/19 passed** using `./tools/Invoke-Unity.ps1 -Task EditMode -ProjectPath "C:/FunGuy's/artifacts/validation/milestone0/FunGuy's"`. The fresh snapshot had no Library cache; global package caches/toolchain remained available, so this is not a clean-machine/CI claim. Development Android APK built successfully after isolating Gradle state. `git diff --check` and PowerShell syntax parsing passed. Android touch/install remains unverified because the emulator's OS failed before game installation. `AndroidReleaseCheck` has not been executed.
- **Production-path impact:** The first application-service boundaries and versioned content/save migrations are in place. Regression tests cover the confirmed stabilization defects and onboarding scene flow. The production roadmap now distinguishes implemented work from completed verification gates.
- **Remaining work:** Validate the APK on a healthy Android emulator/physical device, run the non-development build and check QA exclusion/stripping/navigation, then proceed to canonical rules/stat examples and the formation model. Audit deprecated/unused editor packages in a separate tested cleanup. Canonical balance, full save/account recovery, battle event playback, authored UI and authoritative backend remain the later milestones already documented. No new artwork was needed for this batch.
- Final hash comparison confirmed that runtime code, JSON content and test/assembly definitions in the working project match the clean-tested snapshot. No task-started Unity or emulator process remained after validation.
- An extra whitespace check with a command-local `core.autocrlf=false` override misclassified Windows CRLF endings as trailing whitespace. Re-running `git diff --check` under the repository configuration passed; no line-ending conversion or Git configuration change was made.

## 2026-09-14 — Continue stabilization and prepare team application boundary

- **Status:** In progress. Started `AndroidReleaseCheck` against the previously validated runtime source. A separate, resource-isolated emulator retry is planned after Unity exits.
- **Changes/findings:** Added `RULE_DECISIONS.md` to make unresolved design conflicts and implementation constraints explicit. Requested the proposed two-front/three-back layout clarification; team ownership/selection operations can be extracted without deciding row targeting or balance. The local emulator help confirms software rendering overrides are available for an isolated retry; no saved AVD configuration was changed.
- **Validation:** Release compilation and Android runtime checks pending. Earlier passing results remain historical evidence for the previous batch.

- Added `tools/Start-ValidationEmulator.ps1` and build-guide instructions for bounded, isolated software-rendered Android validation after Unity exits. PowerShell syntax parsing passed; actual execution remains pending the release build.
- Added a root ignore entry for new Unity `.utmp` native-build cache files. Existing tracked historical cache files were not removed or changed intentionally.
- Prepared a team application-service implementation and focused transaction tests outside the Unity source tree while the release build runs. Integration/validation is pending; the current APK build must not be described as including those pending edits.
- The current non-development IL2CPP generated method table retains the Home/Summon/Team/Battle navigation methods. The generated table has no DebugProgressionController or GameplaySmokeTestController methods; runtime navigation still requires the device check.

### Non-development build passes; team service integrated

- The first `AndroidReleaseCheck` succeeded (APK 56,136,490 bytes). Preserved it as `artifacts/AndroidReleaseCheck/baseline-Funguy.apk` with `baseline-build.log`. It contains the prior stabilization source, before the team changes below. Generated/stripped output excludes both QA controller types and retains main navigation handlers.
- Started the bounded emulator retry on `emulator-5580` after Unity exited; device outcome pending.
- Added `ITeamService`/`LocalTeamService`, moved team add/remove/replace/autofill/clear persistence out of `TeamMenuController`, and exposed capacity through the interface. Ownership, known-catalog membership, duplicate rejection, five-unit capacity and all-or-nothing replacement are application responsibilities. No-op changes do not write saves. Ranking ties now use ordinal character IDs consistently in the service and roster UI. Existing save schema/order is retained; no front/back rule was invented.
- Added four focused EditMode transaction tests and a PlayMode team-control persistence scenario. Updated composition/reset and PlayMode isolation for the new service. These additions still require compilation/test execution.
- Build summaries now distinguish actual APK byte size from Unity's aggregate build-report bytes; the next build will use the corrected labels.

### Android baseline touch run and presentation findings

- Isolated software-rendered emulator boot completed in about 91 seconds with Unity stopped. Baseline non-development APK installed successfully; a transient Android System UI ANR prompt was dismissed with Wait. This run does not establish production performance or identify which environment change resolved the earlier boot failures.
- Real adb touch input completed welcome, summon ticket, pull/reveal, team tutorial, first battle and tutorial completion. Home displayed level 1, gold 250 and spores 70, matching the first-clear and one-time tutorial rewards. Options showed only Mute audio and Back, with no QA controls. Screenshots/logs are under `artifacts/device-5580`; force-stop/relaunch persistence check is in progress.
- Device screenshots exposed roster-button labels overlapping the roster summary and the tutorial panel obscuring the battle heading. Adjusted team button sizing/text size and roster spacing, and moved battle heading/team/result content below the overlay. Removed internal character/stage IDs from normal display and changed bright-button captions to dark text. These presentation edits require the updated APK visual check.
- Corrected tutorial hints that claimed front/back placement, manual skill timing or an upgrade flow already existed. Kept the serialized step IDs unchanged for save compatibility; canonical formation/upgrade features remain pending their milestones.
- Force-stop/relaunch returned to Home without replaying the tutorial; gold 250 and spores 70 were retained (`18-relaunch.png`). Stopped the temporary emulator before Unity validation. This confirms baseline Android boot/touch/onboarding/relaunch on this emulator, with physical-device/performance coverage still outstanding.
- Updated source compiled and passed **23/23 EditMode tests**, including all four new team-service tests. Started the expanded PlayMode suite next; updated APK packaging/visual verification still pending.
- Expanded PlayMode suite passed **3/3**, including fresh onboarding, summon resume and team clear/toggle/autofill persistence. `git diff --check` passed. Started a new non-development APK build containing the team service and device-driven presentation fixes.

### Updated non-development APK passes

- Rebuilt with the team service and presentation changes: `artifacts/AndroidReleaseCheck/Funguy.apk`, **56,142,246 bytes**, SHA-256 `C7F4E1829E5A84BF8170294B5DA0A355A4A7902064067C08333309E53ED176E9`. BuildPipeline duration 9m49s. Summary now separates APK bytes from aggregate build-report bytes.
- Bundled aapt reports minimum API 25, target/compile API 36, ARM64, and no `application-debuggable` flag. This is a local non-development APK, not a signed store release/AAB.
- The emulator launcher rejected an early start while Unity was still shutting down, as intended. Retrying after the runner confirmed successful exit; final updated-screen checks are pending.
- Final stripped assembly includes ITeamService/LocalTeamService and still excludes both QA controller types. Release builds also rewrote/deleted pre-existing tracked `.utmp` native-cache files; restored that generated-file churn to its pre-task tracked state, while keeping new cache paths ignored. Runtime source and APK were not changed by this cleanup.

## 2026-09-14 — Team boundary and Android validation delivered

- **Implemented:** Team selection now uses the application service for ownership/capacity validation and atomic persistence; UI layout/contrast and tutorial descriptions match the current prototype. Build tooling reports actual APK size and supports isolated emulator startup. Living build, test, wiring, roadmap and rules documentation updated.
- **Final validation:** Current source passed **23/23 EditMode and 3/3 PlayMode** tests. Updated non-development APK built successfully; its stripped assembly contains the team service and excludes both QA controllers. Real Android emulator touch completed summon/reveal, Clear Team (0/5), Auto Fill (4/5 with this pull), first battle and tutorial completion. Roster labels no longer overlap the summary; battle heading and rewards remain visible below the tutorial panel. Options contains only Mute audio and Back.
- **Persistence:** Force-stop/relaunch returned to Home with gold 250 and spores 70 and no repeated tutorial. Startup was slow on this software-rendered emulator; early captures were blank before Boot/Home finished. Final evidence: `artifacts/device-5580/final-05-team-clear.png`, `final-06-team-filled.png`, `final-07-battle.png`, `final-09-home.png`, `final-10-options.png`, `final-13-relaunch-home.png`, and `final-game.log`. No managed exception or AndroidRuntime fatal entry appeared in the captured filtered game log. Transient Android System UI ANR prompts occurred during startup and were dismissed with Wait; no performance claim follows from this run.
- **Production-path impact:** Earlier Android installation/touch and non-development verification blockers are superseded by successful emulator results. The earlier fresh-import test covered the preceding 19-test source, not these latest additions. Physical devices, clean-machine CI, performance/16 KB validation and signed store delivery remain outstanding. Canonical stat examples and formation rules are next; the two-front/three-back proposal remains unanswered, so row mechanics were not invented. Full roster browsing, authored art/UI, animated battle playback and online authority remain later work.
- **Handoff:** Final APK is `artifacts/AndroidReleaseCheck/Funguy.apk` (56,142,246 bytes; hash recorded above). No artwork was required. `git diff --check` passed, and the task-owned read-only emulator was stopped after verification. Changes remain uncommitted for review.

## 2026-09-14 — Canonical stat rules implementation

- **Authority/scope:** User requested canonical stats based on the stat sheet. Inspected the PDF with extracted text and rendered tables (pages 3, 11, 13; source also pages 1/10). The structured named-stat table contains 14 Tanks, while six class templates/growth tables are available. Older copied narrative elsewhere conflicts with those rows; no complete named non-Tank kits were invented.
- **Source decisions:** Added `COMBAT_RULES.md` with page references, full transcribed tables and explicit conflict resolutions: base + growth × (level - 1); stars multiply HP/ATK/DEF/POT; page 13 SPD = base + 15 × stars with no level growth; caps 100/120/140/160/180/200; C=3000; directed biome damage and separate neutral biome-less identity. Kitchen's lower BST is already in the source rows. Resolutions are implementation decisions under the task, not individual user approvals. Equipment, Core and formation synergy ambiguities stay open.
- **Architecture/content:** Added schema-1 `stat_rules.json`, immutable-input domain catalog, explicit rounding, shared biome/damage functions and canonical factory/campaign integration via `statProfileId`. Catalog validation rejects unsupported versions/classes/biomes, invalid growth, inconsistent BST and evolution curves. Canonical content requires matching identity and omits redundant stats. Existing IDs, saves and summon pools keep their tested legacy stat path until full kits and campaign content are ready; common damage calculation is active now. Future server code can share the pure C# rules without Unity APIs.
- **Verification in progress:** Added independent class/named fixtures, 36 biome matchups, progression boundary/malformed-data checks, catalog snapshot isolation and factory/campaign integration tests. Started EditMode compilation/tests; results pending. Full PlayMode regression follows because shared loading and campaign conversion changed. Existing APK predates this batch.
- **Tool observations:** Bundled Python lacks PyMuPDF; used available pypdf and pypdfium2 for extraction/rendering. An initial table-generation command had a Python reserved-keyword syntax error and was corrected before data was written. All source tables were visually checked; generated inspection files remain ignored under artifacts/stat-rules.
- EditMode completed successfully: **53/53 passed** (30 new canonical-stat cases plus the prior 23). The new fixtures exercise real JSON deserialization, and campaign integration confirms saved six-star progression reaches the calculator. Started PlayMode regression after the editor exited; Android packaging/device checks have not been repeated for this rules-only batch.

### Canonical stat batch validation and handoff

- **Executed results:** EditMode **53/53 passed**, PlayMode **3/3 passed**, using the pinned Unity 6000.3.2f1 runner. New fixtures and all existing tests passed. PlayMode verified fresh onboarding, summon resume and team persistence with the new catalog loading and shared damage path. No Android build/device rerun was performed; the APK linked in the preceding batch does not contain these changes.
- **Evidence:** `artifacts/EditMode/tests.xml`, `artifacts/PlayMode/tests.xml` and their Unity logs. PDF source fingerprint is recorded in `COMBAT_RULES.md` so later sheet edits can be reconciled explicitly. The named stat reference table, all class coefficients and worked examples are documented in the same change set.
- **Limitations/next work:** Existing placeholder roster remains deliberately legacy; the canonical named rows have no newly fabricated skills or summon entries. Named non-Tank stat rows are absent from the structured source table, so DPS/Support/Assassin checks are class templates. Equipment/Core, formation bonuses, active-effect stat composition, complete kits and final balance remain open and must build on these versioned rules. This is a delivered canonical base/evolution/biome rules foundation, not completion of the entire combat milestone.
- **Review:** Documentation is synchronized across COMBAT_RULES, RULE_DECISIONS, PRODUCTION_PATH, BUILD_GUIDE and TESTING. No save migration, artwork generation or dependency changes were needed. Changes remain uncommitted.

## 2026-09-14 — Formation model and presentation implementation

- **Scope/decision:** User requested formations after the stat foundation. Adopted the roadmap's two-front/three-back board as the implementation default. The PDF describes front/back protection and backline bypass but does not fix the slot geometry; this is a documented design resolution, not a claim of an explicit five-slot diagram in the source. Full biome/class/role synergies and cover/taunt/movement abilities remain later combat work.
- **Domain/application/save:** Added stable team-local slot rules, detached formation snapshots, validated place/replace/swap/move operations and save schema 4. Membership stays in activeTeam for existing consumers; formation records positions. Legacy teams fill front left/right then back left/center/right in their prior order. Valid saved positions and empty spaces are preserved; malformed assignments are repaired deterministically without deleting owned units or currency. Membership and positions commit together through ITeamService.
- **Combat/content:** Campaign carries saved positions into battle; unspecified enemy positions fill deterministically around optional explicit slotId values. Validator rejects duplicate/out-of-range enemy slots and waves over capacity. Targeting now supports front/back single targets and rows plus ally-row effects, with living-row fallback for enemy attacks. Death does not collapse the board; ties use stable slot order.
- **UI:** Added visible five-slot formation controls, two-position swap/move, selected-position roster assignment/replacement, removal and roster paging. Battle preview names both rows. Cleared teams no longer silently refill when completed-tutorial players reopen Team, and Start Battle rejects an empty selection. Updated tutorial placement guidance.
- **Validation in progress:** Added migration/sparse-save, placement transaction, deterministic repair, row targeting and damage-survival fixtures plus a PlayMode placement/paging/reopen scenario. EditMode compilation/test run started. UI visual/device validation still pending; previous APK does not represent this source.
- First EditMode pass succeeded **64/64**. Follow-up inspection found that QA wave construction ignored optional enemy positions; centralized that conversion in CombatUnitFactory for both campaign and QA and added explicit-slot JSON/validation coverage. Final EditMode pass succeeded **65/65**. PlayMode regression is running. `git diff --check` passed; tracked .utmp caches were clean before any new packaging.
- Final PlayMode passed **4/4**, including the new placement/paging/sparse-board scene-return scenario and the existing onboarding flows. Started AndroidReleaseCheck against this tested source to verify phone layout/touch input. No application-source edits are planned during packaging; current visual result remains pending.
- Android compilation emitted CS8785 for AttributeBasedFieldGenerator in Assembly-CSharp. The same warning is present in the preserved baseline-build.log, and the C# build graph completed successfully; it is a pre-existing generator warning, not evidence of a new formation compile failure. Final packaging/runtime outcome remains pending.
- The current stripped release assembly retains FormationRules, ConfigureFormation, SwapSlots and DamageCalculator and excludes both QA controller types. Native packaging is still running; this assembly inspection does not replace the pending installed-APK check.
- A requested 45-second build wait returned after roughly 53 minutes of wall time. On recheck Unity was still running and fresh native compiler workers were starting; the APK still had the preceding build timestamp. This is an observed execution delay, not a confirmed source failure or a completed new build. Continued the same build rather than overwriting its state or claiming the old APK as current.

### Formation APK built

- AndroidReleaseCheck succeeded with the canonical stat and formation source: `artifacts/AndroidReleaseCheck/Funguy.apk`, **56,168,852 bytes**, SHA-256 `0424348347ABA9F9E0CE9B695AD086A1DA8A103589563622ACBE237DE4D5AB6C`. Non-development Unity 6000.3.2f1 build. Reported duration 1h11m includes the long observed execution gap and is not a performance benchmark.
- Unity exited successfully. Restored only generated tracked .utmp cache churn after checking that path was clean before packaging; new cache files remain ignored. Started the task-owned read-only software-rendered emulator for phone layout/touch checks. Existing tests remain 65/65 EditMode and 4/4 PlayMode; native verification pending installation.

### Formation Android verification and delivery

- **Installed artifact:** The new APK installed and launched on the isolated `emulator-5580`. Bundled aapt reports minimum API 25, target/compile API 36, ARM64 and no application-debuggable flag. This remains a local test APK, not signed store delivery.
- **Actual touch/visual checks:** Completed fresh summon/reveal and onboarding with Ashmote Kira as the fourth owned fighter. Placed Kira at back right, moved her to back center, swapped Puffmage Orbi and Mosswhisper Luma across rows, then removed Kira without losing ownership or shifting other fighters. The board, labels, guidance and buttons were readable without overlap. Battle preview reflected the changed rows; the team cleared both first-stage waves and completed the tutorial with gold 250/spores 70. Evidence: `artifacts/device-5580/formation-07-placed.png`, `formation-08-move.png`, `formation-09-swap.png`, `formation-10-remove.png`, `formation-11-battle.png`, `formation-12-result.png`, and `formation-15-home.png`.
- **Persistence:** Returning to Team retained the changed formation. Moved Luma from front right to back right, leaving front right and back center empty, then force-stopped/relaunched the app. Home retained currency and tutorial completion; Team retained all three exact positions and both gaps (`formation-17-sparse.png`, `formation-18-relaunch.png`, `formation-19-persisted.png`). The sixth-owned-unit paging interaction is covered by PlayMode; this native run had four owned units and verified the disabled single-page controls only.
- **Limits/findings:** A transient Android System UI ANR appeared during initial startup and recovered after Wait, as in prior emulator runs. No managed exception or AndroidRuntime fatal entry appeared in the captured filtered `artifacts/device-5580/formation-game.log`. Software rendering/startup delays do not establish physical-device performance. Physical devices, clean-machine CI, 16 KB validation and signed AAB delivery remain outstanding.
- **Handoff:** Rechecked final test XML (65/65 EditMode, 4/4 PlayMode) and APK SHA-256 against the build result above. No application source changed after those tests. Stopped the task-owned read-only emulator after verification. Formation rules, decision register, production path, build/test and UI wiring guides reflect the implementation. Battle events, representative canonical kits, cover/taunt and explicit formation synergy semantics remain subsequent combat-foundation work. Changes remain uncommitted for review.
- Final `git diff --check` passed; no Unity/emulator process remained after cleanup. Git reported normal LF-to-CRLF working-copy notices, with no whitespace errors.

## 2026-09-14 — Full hex-board correction from user reference

- **Evidence and revised path:** The user supplied a landscape gameplay reference and explicitly corrected the five-space board: placement must use a full board with multiple empty options. Preserved the source at `docs/references/full-board-reference.png`. The prior assumption conflated team size with board size and is superseded. Asked for preferred dimensions; meanwhile adopted 12 spaces per side (three depths/four staggered lanes), retaining five fighters as a documented working default. This is not a claim of explicit approval of the exact dimensions.
- **Domain/application:** Separated fighter capacity from available spaces, added stable depth/lane IDs, preserved placement transactions and blocked sixth-member placement into empty cells while allowing replacement/moves. Front/rear targeting now traverses three depths; row effects stop at one occupied depth, and ally-row targets the actor's exact depth. Lane range, movement and adjacency remain unimplemented.
- **Compatibility:** Save schema 5 maps old 0/1/2/3/4 positions to 0/2/8/9/11 before new-board validation; older saves pass through that map. Invalid old indices cannot become valid new cells accidentally. Legacy enemy content slot names remain mapped aliases. Old ownership, currencies, tutorial and reward claims are preserved.
- **Presentation:** Team shows twelve selectable hex spaces; Battle shows a mirrored 24-space starting-deployment preview using the shared factory/assignment rules. Preview is explicitly the first enemy wave's starting state, not live playback. Code-rendered hex silhouettes need no generated art. Final landscape presentation and art remain later work.
- **Validation in progress:** Added all-cell/capacity/replacement, middle-depth fallback and v4-to-v5 migration fixtures; expanded PlayMode to use outer and middle spaces plus optional UI screenshot capture. EditMode compilation/tests running; PlayMode and visual checks follow. Existing Android APK still contains the preceding five-space implementation and must not be represented as the corrected board.
- **Executed checks:** EditMode passed 68/68. Initial PlayMode passed 4/4; ordinary ScreenCapture produced no files in batch mode despite enabled graphics. Replaced that optional inspection path with a render request over the actual canvases, restoring their state afterward; the subsequent PlayMode run passed 4/4 and produced Team/Battle PNGs at 1080x2400. Visual inspection confirms twelve cells per side and no board/control overlap; these are editor canvas renders, not device screenshots.
- **Finding and fix:** Rendered Team revealed that TutorialSpotlightController cleared the shared hint every frame after onboarding. Team now retains exclusive ownership of its placement/error instructions while tutorial messaging stays in the overlay. Added a PlayMode assertion that the selected Rear 4 hint survives settled frames. Re-running PlayMode/captures for this final UI fix; no domain changes after the passing EditMode run.
- **Final verification:** Final PlayMode run passed 4/4 after the hint ownership fix; final Team render shows persistent placement/swap instructions and twelve readable, distinct cells with five selected fighters. Battle render shows mirrored twelve-cell sides and the first-wave starting-deployment label. Evidence: `artifacts/PlayMode/screenshots/hex-team.png`, `hex-battle.png`, PlayMode tests.xml and EditMode tests.xml (68/68). Capture uses a temporary camera and render texture; it is editor layout evidence only. Unity exited; no APK rebuild or new device claim was made for this correction.
- **Delivery:** Updated FORMATION_RULES, RULE_DECISIONS, PRODUCTION_PATH, BUILD_GUIDE, test coverage and UI wiring documentation; preserved the user's reference under docs/references. The old five-space APK remains historical and should be rebuilt before a new Android handoff. Exact board dimensions remain adjustable separately from team capacity. Work remains uncommitted.
- Final whitespace check passed (exit 0); no Unity process remained and tracked .utmp caches were unchanged.

## 2026-09-14 — Remaining-work review

- User asked what remains before the game is done. Reviewed current production milestones, rules register and latest delivered/tested changes; separated the first complete playable slice from the planned commercial gacha release. Added a current completion outlook to PRODUCTION_PATH.md so historical baseline findings/test counts do not obscure today's status.
- Current foundation: local onboarding/summon/campaign/save operations, canonical stat references and full-board placement. Remaining work covers unresolved design choices, battle events/effects/synergies, animated presentation and authored art/audio, complete character kits/campaign/boss, progression/equipment/Core and balance, online authority/accounts/purchases, device QA and launch operations. Exact board dimensions, team limit, orientation and combat controls still need a settled product contract; prototype defaults are not final approval.
- Evidence is the preceding recorded 68 EditMode/4 PlayMode results and editor board renders; no new tests or APK build were run for this documentation review. Existing APK still predates the twelve-space correction. No new completion percentage, timeline or scope expansion was claimed. First target stays a coherent 15-30 minute playable slice, with later social modes/platforms deferred.

## 2026-09-14 — Sequential build order, item 1 started

- User requested working through the stated build order one by one. Began with the gameplay contract rather than treating the earlier twelve-space/five-fighter and portrait/automatic defaults as final product decisions. Requested three preferences together: board/team dimensions, orientation, and manual signatures with Auto versus fully automatic combat.
- Prepared a concrete decision packet in RULE_DECISIONS.md and added sequential execution status to PRODUCTION_PATH.md. Confirmed full-board placement and stat-sheet authority; proposed landscape and automatic basics/player-queued signatures with Auto based on the reference, explicitly pending user preference. Recorded the next-eligible-action interpretation for proposed manual controls and separated movement/target-selection mechanics from the image's visual cues.
- Source inspection: BattleSim currently consumes the whole battle synchronously, automatically chooses an eligible signature, uses a 1000-point action threshold and preserves gauge overflow. The extracted sheet says reset the gauge to zero, a pre-existing RULE-007 difference to resolve in combat work. Current 20 start-turn, 5 damage, 10 damage-taken and nonstacking 15 kill energy values match the sheet's stated amounts; per-hit/multi-target lifecycle semantics still need explicit fixtures. Runtime UI uses a 1080x1920 reference canvas. These findings inform the upcoming battle-session/event/command boundary.
- Item 1 remains in progress while preferences are pending. No runtime settings, balance or saved data changed, and no test/build run was needed for this design/documentation batch. Prior validation remains 68 EditMode/4 PlayMode with editor hex-board renders; the existing APK still predates the full-board correction. No later item was marked complete or silently begun.

## 2026-09-14 — Item 1 confirmed; item 2 command/event foundation

- **User decisions:** Explicit answers confirmed 12 spaces/five fighters, landscape like the reference, and manual signature skills plus Auto. Marked gameplay-contract item 1 complete and synchronized RULE_DECISIONS, FORMATION_RULES and PRODUCTION_PATH. These are now approved product choices. Landscape layout and visible skill/Auto controls remain implementation work; the existing UI is still the portrait prototype.
- **Implemented item 2 increment:** Added public BattleSession with detached unit/status/skill snapshots, one-action stepping, manual queue/cancel/Auto operations, accepted-command history at action boundaries, immutable events/state, and explicit Victory/Defeat/Draw/Timeout outcomes. Instance IDs distinguish identical enemy content and enemy waves while preserving player identity. Terminal sessions are no-ops. Existing synchronous RunBattle drains the same engine with Auto enabled for compatibility.
- **Integration:** Campaign results include seed, rules version, outcome and ordered per-wave initial-state/event traces; wallet/reward commits remain in the campaign application service. Battle UI now distinguishes draw and timeout messages. Public manual sessions are not yet connected to an interactive multi-wave application lifetime or player-facing buttons; that integration is next, before animation.
- **Rule resolution:** Restored the sheet's zero-reset action gauge, superseding the previously proposed overflow retention. Documented start-turn/cooldown/status aging, per-damaging-effect energy, kill replacing hit energy, queued eligibility and nonrecursive reflection in BATTLE_SESSION.md. Complete canonical statuses/passives/cover/taunt/synergies remain later item-2 work. No save schema or permanent formation change was needed.
- **Validation:** First EditMode run passed 76/76, including command eligibility, outcome/death cases, identity/isolation, gauge/energy and command replay. Follow-up inspection added random targeting/status effects to the replay fixture, stepped-versus-legacy equivalence, skipped-turn timing and heal/shield/status snapshot checks; final EditMode run is in progress. PlayMode follows because campaign execution changed. Current APK still predates these changes and the full-board correction.
- **Practical limits:** Replay tests use the same runtime/seed/original inputs, not cross-platform determinism; System.Random replacement and a full versioned encounter payload remain online-verification work. Status events retain legacy lack of original-caster attribution. Event history adds allocations that need device profiling with the animated slice. Documentation now distinguishes the implemented session boundary from remaining combat/presentation work.
- **Final verification/delivery:** Final EditMode passed 80/80 and PlayMode passed 4/4. Existing onboarding, summons, team persistence and first battle still pass after the gauge/session/campaign changes. Random targeting/status command replay, automatic-adapter equivalence and skipped-turn/heal/shield state fixtures passed. No runtime source changed after these runs. Whitespace check passed; Unity exited and tracked .utmp caches were unchanged. No new APK or device run was performed. All work remains uncommitted.
- **Build-order status:** Item 1 complete. Item 2's session/command/event foundation complete; item 2 as a whole remains active. Next is an application-owned interactive campaign lifetime for commands, wave transitions and transactional reward completion, followed by canonical effects/passives/synergies. Landscape layouts, visible signature/Auto controls and animation remain item 3. BATTLE_SESSION.md is the living contract; guides and rules/roadmap records have been updated alongside the implementation.

## 2026-09-14 — Step 2 completion work: interactive campaign and combat semantics

- Added application-owned CampaignSession with step/wave progression, seed and encounter identity, cancellation, complete-wave settlement and command/final-state traces. Legacy Run drains the same session. Settlement reads the latest save, awards first clear atomically, and remains retryable after a failed write. Catalog/reward inputs are captured at start.
- Added battle-actions-v2 effects, source-bearing status snapshots, passives, targeting/cover/movement, five biome and six class bonus families, and fourteen representative role pairs. Added validated stat-sheet example content outside ownership/summon pools. COMBAT_EFFECTS.md records exact rules, source conflicts, omitted tuning values, and content-specific mechanics still assigned to the later roster milestone.
- Existing 80 EditMode regressions passed after the initial engine extension. The first expanded test run stopped at a C# target-typed params-array test syntax error; corrected to explicit EffectDef. Expanded validation is running; no new pass claim yet. No Android build/device check has run for this batch.
- Findings: source role prose conflicts with named Cover kits; use explicit adjacent Cover versus Snow-Bank Hermit's full-side first-hit interception. Poison/Burn/Bleed require different units/timing. Biome/class tiers and owner-turn interval choices were unspecified and are now versioned. Legacy Flare Bloom Burn changed to flat 6 damage; no save migration.

### Step 2 validation and review follow-up

- Expanded tests exposed Unity JsonUtility materializing omitted canonical baseStats/growth as zero-filled nested objects. The canonical validator now accepts absence/empty values while rejecting nonzero/NaN overrides; resource-backed reference kits cover this import boundary. The initial expanded run was 84/97 passing with 13 shared catalog-load failures; after the fix, all 97 passed.
- Additional interaction checks brought EditMode to 108/108, and PlayMode passed 4/4. A final event review found Attrition notifications reported the active attacker rather than the actual defender/status source, and permanent-status event amounts reported 1 instead of -1. Corrected both and added a source/duration regression. Final-source checks are running.
- Not Alone now grants Survivor energy on both direct and intercepted damaging hits on its living Cover partner; only intercepted damage receives its reduction. Bonus read models expose supported active families/pairs without announcing nonexistent biome-less or Enemy-class bonuses. Fighter snapshots expose basic/signature IDs for step-3 controls.

### Step 2 complete — final source validation

- Final source: **109/109 EditMode passed** via tools/Invoke-Unity.ps1 -Task EditMode; **4/4 PlayMode passed** via -Task PlayMode. Fresh results/logs are artifacts/EditMode/tests.xml and artifacts/PlayMode/tests.xml plus their unity.log files. PlayMode covers the existing onboarding/campaign adapter and formation UI callbacks; it does not certify new landscape visuals.
- git diff --check passed. New Unity scripts/content/tests have generated .meta files. No tracked ProjectSettings, performance-test resources or .utmp changes remain from these validation runs. No new APK, device run or visual screenshot capture was performed; the existing APK predates the full board and v2 engine.
- Updated BATTLE_SESSION.md, COMBAT_EFFECTS.md, FORMATION_RULES.md, COMBAT_RULES.md, RULE_DECISIONS.md, BUILD_GUIDE.md, test guidance and PRODUCTION_PATH.md to match implementation, actual source decisions and validation. Sequential step 2 is complete at its representative combat-foundation scope.
- Next: step 3, landscape presentation using CampaignSession and ordered events, with visible manual signature/Auto controls, HP/status readability and animation. Full authored roster/boss balance, the ten listed kit-specific role-pair mechanics, equipment/Core/progression, online authority and release/device gates remain later steps. No save, summon-pool or permanent formation migration was introduced in step 2.

## 2026-09-14 — Step 3 landscape presentation implementation

- Authored reusable BattleScreen/BattleFighter resource prefabs with explicit editor rebuild tooling, TextMeshPro labels, 24 hexes, fighter/HP/energy/status views, signatures, Auto, pause, speed, reduced motion, inspection and result controls. CampaignSession remains the sole battle/reward authority; the UI consumes ordered event snapshots. Navigation cancels unfinished encounters.
- Added a safe-area-aware 1600×900 design surface and landscape adaptations for Home, Summon/reveal, Team, Options and tutorial overlays. Player settings now permit both landscape orientations and disable portrait.
- Used the built-in image generation tool (imagegen skill) for an original kitchen backdrop, saved as Assets/_Game/Resources/Presentation/kitchen-battlefield-v1.png. The tool exposes no explicit model selector; no claim is made that a specific 2.5 model was selected. Fighters use editable code-native pixel silhouettes; unique roster art remains content work. Prompt/provenance will be recorded in BATTLE_PRESENTATION.md.
- Presentation prefab generation compiled and exited successfully. Updated onboarding validation for asynchronous playback and added interactive control/settlement/navigation coverage plus optional landscape captures. Step-3 tests and rendered layout review are pending; prior 109 EditMode/4 PlayMode results certify step 2 only.

### Step 3 verification findings

- EditMode passed 109/109 and expanded PlayMode passed 5/5, including live session controls and rewards. The first launch failed to compile the new TMP-facing PlayMode test until Unity.TextMeshPro was referenced explicitly; the subsequent run passed.
- Visual review found a defect the callbacks did not detect: custom MaskableGraphic subclasses lacked CanvasRenderer, leaving hexes and mushroom portraits invisible while labels rendered. Added required renderer components, a component regression assertion and rebuilt the prefabs. Tightened stacked fighter labels so adjacent occupied lanes do not collide.
- Added a kitchen-backed tutorial welcome surface; the old welcome scene rendered only its overlay against black. Surrounding Summon/reveal and Team landscape captures fit their controls. Further captures and final-source validation follow these corrections.

- Follow-up renders confirmed all 24 hexes and ten fighters. They exposed numeric HP labels too short for TMP line metrics and occupied-cell labels competing with fighter names. Added backed HP labels with sufficient height, hide only occupied cell captions (restoring them after movement), and show In battle/Paused on the disabled Start control. Added a numeric-HP mesh assertion. Reset transient visual effects at wave initialization. Inspector and result panels render legibly; the welcome scene now has its own art/title. Final capture suite and Android build are next.

### Step 3 final editor validation

- Final functional source passed **109/109 EditMode** and **5/5 PlayMode**. Captures at 2400×1080, 1920×1080 and 1600×1200 show the full board, readable numeric HP, visible fighter silhouettes, separate empty-cell captions, signature controls, inspection and results. Menu/onboarding captures also passed visual review. The five-versus-five frame is saved in docs/references/battle-landscape-step3.png; other captures remain under artifacts/PlayMode/screenshots.
- Restored the TMP fallback font asset after Unity's dynamic glyph cache serialized test-generated changes. Preserved intended landscape PlayerSettings changes and all pre-existing working-tree work. Whitespace validation passed after removing a trailing controller blank line. No gameplay logic changed after the final tests.
- Updated the production path, session/formation/rule decisions, build guide and UI/test guidance; BATTLE_PRESENTATION.md records boundaries, controls, prefab workflow, image prompt/provenance and later art/audio replacement scope. AndroidReleaseCheck is running on this source; device evidence is still pending.

### Step 3 Android build

- AndroidReleaseCheck succeeded with Unity 6000.3.2f1, IL2CPP/ARM64 and Development=False. Fresh APK: artifacts/AndroidReleaseCheck/Funguy.apk, 59,932,358 bytes; build duration 12m52.7s. This APK includes the full board, battle-actions-v2 and landscape presentation, superseding the older pre-board APK. This is a local release-configuration APK, not a signed store/AAB delivery.
- Started the read-only software-rendered validation emulator on dedicated serial emulator-5582. Touch/lifecycle verification is in progress; prior device-5580 evidence remains separate.

### Step 3 complete — Android touch and persistence verification

- Fresh-account release APK passed touch onboarding, the free summon/reveal, placement of Ashmote Kira on Rear 4, and transfer of that four-fighter deployment to the full landscape battle. Native captures show all 24 hexes, readable HP, energy/status displays and signature cards.
- Touch-verified Start, Pause/Resume, signature queue/cancel feedback, fighter inspection/close, Auto, 1.5× speed, reduced motion and Finish on Auto. Both waves completed with a victory and first-clear reward confirmation. Completed onboarding reached Home with 250 gold and 70 spores. Release Options contained only audio/back, with no development resource tools.
- Force-stopped and relaunched the app. Home retained 250 gold/70 spores and completed onboarding; re-entered Battle retained four fighters including Rear 4, kept reduced motion enabled and reset Auto to off as designed. Evidence: artifacts/device-5582/step3-05-game.png through step3-19-restored-battle.png; step3-game.log and step3-system-crash.log. The isolated read-only emulator was stopped afterward.
- Limits: this API-36 x86_64 software emulator used ARM64 translation and had system-service startup stalls/restarts and a Digital Wellbeing ANR dialog before the game became usable. These observations do not certify startup speed, frame rate, stability on physical hardware, notches or reverse landscape rotation. No game exception was found in the captured Unity log. Menu gutters retain their existing camera background color and menu/reveal art is still prototype styling; final menu/character/skill art and audio remain content/polish work. Battle controls and information remained readable and usable.
- Step 3 is complete at its landscape presentation scope. Final evidence: 109/109 EditMode, 5/5 PlayMode with three landscape aspect ratios, successful non-development Android APK, and the native touch/persistence checks above. All material changes/findings are reflected in the living guides. No commit or staging was performed. Next is step 4: authored roster, campaign/boss and one upgrade loop; physical-device/release gates remain later work.

## 2026-09-14 — Step 4 started: level upgrade loop

- Reviewed the production path, existing content, save/application boundaries and stat-sheet source. The structured named rows in this PDF cover Tanks; other classes have base/growth templates and conflicting older narrative examples. Asked whether to author draft kits for existing characters from those templates or use additional sheets. No named roster conversion or summon-pool change has been made in this increment.
- Started step 4 with its first independent deliverable: a Team workshop to inspect an owned fighter, preview next-level stats/cost, spend gold, save and carry the level into combat. Added a pure C# versioned level-cost catalog and IUpgradeService/local transactional adapter. The existing CombatUnitFactory supplies both preview and battle stats, including canonical profile/evolution support without changing the existing prototype scale.
- Source finding: the PDF supplies growth/evolution caps and Core/equipment tables, but no character level-up price curve. slice-levels-v1 is explicit slice tuning: one level for 25 + 10*(current level - 1) gold, with purchases capped at level 20. This purchase cap does not replace the sheet's 100-200 evolution caps. Existing higher-level saves retain their levels. XP, stars, copies, gear, Core, premium spores and stage claims are untouched; the saved level field already exists, so no schema migration is required.
- Transactions re-read current state, validate unique ownership, progression/cost/version/expected level and affordability, then publish one complete snapshot. Failed writes cannot publish a partial deduction; duplicate/stale expected-level requests cannot buy the following level. UI presents acquisition tier separately from evolution and shows current level on Team cards. No new external service or image generation was needed.
- Validation pending: domain tests cover successful/shared-stat upgrades, canonical evolution, caps, stale calls, intervening writes, malformed catalogs and storage failures. A new PlayMode flow covers reward gold, visible upgrade controls, persisted reload and battle stats. Running EditMode now; PlayMode and landscape render review follow. Last Android APK still contains step 3 only.
- Tooling finding: PyMuPDF is absent from the bundled Python runtime. Used available pypdf for extraction and pypdfium2 for read-only page rendering instead; no dependency installation or PDF modification.

### Step 4 upgrade verification follow-up

- EditMode passed 117/117. The first PlayMode run passed 5/6: the new workshop test incorrectly assumed onboarding starters begin at level 1, but the existing tutorial seeds them at level 3. Corrected the fixture to test a real level 3→4 upgrade (45 gold), without altering existing starter progression. Corrected the cost walkthrough accordingly.
- Reviewed the distinction between permanent workshop stats and encounter formation bonuses: the starter's two Forest fighters add 5% HP at battle start. Added an explicit before-formation-bonuses label and changed the battle integration expectation to include this existing bonus. Added UI coverage for the second roster page, insufficient gold, cap and empty-roster states, and prevented repeated refreshes from duplicating shortfall feedback. Re-running PlayMode/captures on these UI/test corrections.

### Step 4 first increment complete — upgrade workshop

- Final domain checks: **117/117 EditMode passed**. Final UI checks: **7/7 PlayMode passed** after correcting the test's starter-level assumption. The real reward→workshop→save reload→battle flow upgraded Orbi from level 3 to 4 for 45 gold and retained formation; battle received the expected upgraded HP plus the existing two-Forest bonus. Additional UI coverage passed paging, disabled unaffordable purchase/no mutation, cap and empty roster.
- Reviewed 2400×1080 preview/success/shortfall and 1600×1200 tablet renders. All five stat rows, price, currency, evolution/rarity, selection and controls are visible without overlap. Captures are under artifacts/PlayMode/screenshots/step4-upgrade-*.png; the success view is preserved at docs/references/upgrade-workshop-step4.png. This is editor rendering/callback/persistence evidence, not native Android touch or process-kill evidence.
- Restored the exact pre-run TMP fallback font bytes after dynamic-glyph generation. New source/content/test files have Unity-generated meta files. Whitespace check passed, Unity exited, and pre-existing changes remain intact. No staging or commit performed. Updated LEVEL_PROGRESSION, PRODUCTION_PATH, RULE_DECISIONS, BUILD_GUIDE and UI/test guidance with the delivered behavior and limits.
- Step 4 remains active: this increment completes the gold-to-level path, not the full roster/campaign milestone. Next is the 8–10-unit authored roster/content transition and campaign/boss balance, followed by timed slice/Android verification. The existing release-check APK still contains step 3. The optional roster-source preference has not received an answer during this increment; no character identities, summon pools or canonical source profiles were changed while building the independent upgrade path.

## 2026-09-15 — Step 4 roster and campaign content increment

- Continued under the user's request. The earlier optional roster-source question has no answer/additional sheet, so the working direction is explicitly draft kits for the ten existing characters. Kept their names, stable IDs and acquisition pool buckets. No source Tank profile has been relabeled as one of these characters; no claim that the draft kits are named rows from the sheet.
- Added class-growth-v1: authored base distributions with the existing canonical class growth/evolution calculator. Ten slice-roster-v1 kits cover six classes and five biomes, each with a separate basic, signature and passive. Tank distributions are explicit 600 BST drafts; Kitchen Zenith has an explicit 575 BST distribution. Existing levels/stars/copies/XP/formation and pity/history stay in place; this is a balance/content update, not an ID replacement. Standard stat/evolution bounds apply; no silent level clamping or evolution purchase.
- Authored seven kitchen campaign stages with explicit hex placements, tips, new enemy roles and a Cauldron Keeper boss (opening shield, periodic pressure, team Burn signature). Existing stage IDs 1–5 and first-clear claims remain, so new reward amounts do not retroactively regrant old clears. Added encounter version to session/results and a Home campaign picker exposing locked/new/cleared states and practice-versus-first-clear rewards.
- Workshop kit descriptions are now scrollable and summon reveal/summary uses acquisition tiers instead of implying evolution stars. All new effects use already-implemented handlers; no speculative Core/equipment or backend system was introduced.
- Validation pending: added roster/source-growth/content-preservation checks and a 20-seed campaign harness that spends only earned gold on the original three starters. It must establish a route through the boss independent of summon luck. Existing tests with synthetic owned units now explicitly set evolution 1 instead of relying on an omitted zero that the old legacy stat path ignored. Canonical profile override tests clear the newly explicit draft model before testing the profile path.

### Step 4 content validation follow-up

- Initial expanded EditMode run: 117/120 passed. The 20-seed real-service route cleared all seven stages at starter levels 3→9 using earned gold, without summons. Three legacy fixture failures referenced the old 11-wave count, old enemy growth/HP or omitted the now-required stat catalog. Updated those expectations to the authored 14-wave chapter and explicit growth; no engine rule was changed to satisfy a test.
- Added execution coverage for each kit's basic/signature/passive and a comparison against an unupgraded starter boss attempt. Adding PlayMode coverage for campaign locks, automatic first-uncleared selection, boss entry/victory, rewards, returning selection and practice labeling.
- Corrected the test report path (two parent directories from Application.dataPath, not three). Copied its initial CSV into repository artifacts and removed only that test-generated outside-workspace CSV. Final harness output stays under artifacts/step4-content.

- Kit execution checks passed for all ten basics/signatures/passives. The expanded run was 121/122: the negative-control boss scenario revealed the level-3 starters also won 20/20 without upgrades, so the first boss draft provided too little pressure. Raised only the keeper's base HP 2100→3300, ATK 75→140, DEF 190→230, SPD 100→110 and POT 90→140. Retesting both the earned-upgrade route and the unprepared comparison; ordinary stages and player rules are unchanged by this tuning.

### Step 4 final content checks and UI review

- Retuned source passed **122/122 EditMode**. The earned-gold starter route won 140/140 stage runs (20 full chapters), finishing with level-9 starters and gold remaining; the level-3 boss comparison lost 20/20. Final stage action counts were 9, 18, 18, 24, 22, 30 and 47–52 respectively. These scripted encounters are short; the 15–30 minute human-play target is not established and needs pacing/UX playtests rather than a fabricated duration claim.
- PlayMode passed **8/8**, including campaign locks, automatic first-uncleared selection, boss entry/victory, +600 gold saved, remembered selection and practice labeling. Existing onboarding, all board controls and the upgrade/reload/battle path still pass with the new class-growth roster.
- Workshop render shows readable basic/signature/passive descriptions and source-growth stats. Campaign render exposed faint Home controls through a 0.99-alpha panel; changed its background to fully opaque. Re-running the final capture suite for this visual fix, then building Android. No engine/content changes after the 122-test passing run.

### Step 4 source-alignment correction before Android

- The opacity-only UI run passed 8/8. Final source review identified a carried-forward prototype conflict: R Orbi/Kira were still elemental despite the sheet's biome-less Rare rows. Corrected both draft bases to 500 BST and Biome-less while preserving names, IDs, rarity buckets, pull weights and ownership. Orbi's draft base is 90/120/70/110/110; Kira's is 100/70/90/100/140 (HP/ATK/DEF/SPD/POT). This supersedes the earlier all-non-Kitchen-600 statement for Rare fighters.
- Added content validation for R biome-less identity and draft 500/575/600 budgets, updated independent stat fixtures and the upgrade-to-battle expectation (the starter team now has only one Forest unit, so no Forest HP tier). Retesting both campaign balance controls and the UI before Android packaging. No duplicate first-clear rewards or progression changes are introduced.

- Final Rare-aligned source passed **122/122 EditMode**. The full earned-upgrade chapter route remains 140/140 victories, and unupgraded starters still lose the boss 20/20. No enemy retuning was required after the R identity/base correction. The launch guard correctly rejected one attempted EditMode start while the preceding PlayMode Unity process was still shutting down; waited for its successful 8/8 completion and then ran the suites sequentially. Final Rare-aligned PlayMode/captures are now running.

### Step 4 final UI evidence and packaging

- Final Rare-aligned PlayMode passed **8/8**. Reviewed the new 2400×1080 phone and 1600×1200 tablet campaign captures, workshop kit panel and boss victory. Campaign is fully opaque; descriptions and controls remain readable. Copied selected evidence to docs/references/campaign-step4.png, roster-kits-step4.png and boss-victory-step4.png.
- Final Rare-aligned action counts across the 20-seed route are 12, 24, 32, 28, 40, 28–30 and 59–64 for stages 1–7. These supersede the pre-Rare-correction action counts above; the 140/140 victory and 20/20 unprepared-loss results remain unchanged.
- Restored the TMP fallback font's exact pre-PlayMode bytes after generated glyph-cache changes. Started AndroidReleaseCheck against the final tested gameplay/UI source; packaging outcome is pending. Updated living scope/progression guidance to distinguish delivered content from guided-upgrade polish, human pacing and native Android validation.

### Step 4 content delivery — final build result

- AndroidReleaseCheck succeeded with Unity 6000.3.2f1, development disabled, in 12m29s. Fresh output: artifacts/AndroidReleaseCheck/Funguy.apk, **59,965,964 bytes**, written 2026-09-15 00:43 local. ZIP inspection confirms lib/arm64-v8a/libil2cpp.so. SHA-256: 2E47D76C939AFE0DF84B5B6C31B688E80D14F7F573115A37E58A17681C2D69C2. The summary and Unity log are beside the APK. This supersedes the step-3 APK at that path; it is a local release-configuration APK, not a store release/AAB.
- Final validation is **122/122 EditMode, 8/8 PlayMode**, reviewed phone/tablet captures, 140/140 upgraded stage wins and 20/20 unupgraded boss losses. No gameplay/UI source changed after these passing suites. No native Android install/touch or timed human playthrough was performed for this increment.
- Unity exited. Source whitespace check passed under repository Git settings, excluding generated .utmp caches; a diagnostic override disabling autocrlf had incorrectly treated CRLF endings as whitespace, so it was discarded without rewriting files. TMP fallback matches its pre-run bytes. New scripts/tests have Unity-generated .meta files. Existing work and generated native cache state were left unstaged; no commit or broad reset performed.
- Updated SLICE_CONTENT, PRODUCTION_PATH, LEVEL_PROGRESSION, BUILD_GUIDE, COMBAT_RULES, RULE_DECISIONS and UI/test guidance. Saved screenshots and a concrete pending Android acceptance route. Step 4 remains active for guided first-upgrade polish, native integrated playtesting and measured pacing; bespoke portraits/animations/audio remain content/polish work. Later progression and online milestones have not been started.

## 2026-09-15 — Step 4 guided upgrades and Android continuation

- Inspection found that onboarding ended after three generic reward/team messages without directing players to the new workshop. Reused tutorial step IDs 7–9 for first-clear policy, stat/cost guidance and a Team workshop walkthrough. Step 9 now opens Team on both entry and resume and highlights Upgrade fighters; Complete remains ID 10. Existing completed tutorials and save schema remain unchanged.
- Workshop opening hides the guide so it cannot obscure the preview/purchase controls; closing restores a campaign continuation prompt. Spending remains optional, including at zero gold. Guidance performs no economy mutation; the existing IUpgradeService remains the sole purchase operation.
- Extended the complete onboarding test through a persisted purchase and added a saved-step/zero-gold resume test. Validation and updated Android packaging/runtime checks are pending. Human pacing remains distinct from automated or agent-operated timing.

- PlayMode passed **9/9**, including the purchase path and zero-gold resume. Reviewed both new captures: the Team guide fits, the workshop button is highlighted, and the banner disappears during the purchase. Saved docs/references/upgrade-guidance-step4.png. Restored the exact pre-run font asset and started AndroidReleaseCheck. Combat/content rules are unchanged; the prior 122/122 EditMode and 20-seed balance evidence remain applicable to those unchanged systems, rather than being claimed as a new run.

- AndroidReleaseCheck succeeded: Unity 6000.3.2f1, development disabled, 7m09.8s, **59,967,496-byte APK**. SHA-256 A977315921C51BF8FF99FCE3A70CBBC3FE76331229165EA9DF857E72C04AE17B. This replaces the earlier content APK at artifacts/AndroidReleaseCheck/Funguy.apk with the guided-upgrade source. Started the isolated read-only emulator on serial emulator-5584 for native acceptance; runtime results remain pending.

- Native progress: release APK installed on emulator-5584 and its temporary app data was cleared for a fresh run. Android System UI showed an ANR before gameplay; selecting Wait and dismissing the immersive-mode notice allowed startup. This API-36 x86_64 emulator uses ARM64 translation/software rendering, so startup and agent-operated timing are not physical-device performance or human pacing evidence.
- Touch-completed free summon (Kira), moved the three starters to Front 2/Rear 2/Rear 4, won both first-stage waves with Auto off and reached the new guide. Workshop hid the banner; purchased each starter's level 3→4 for 45 gold (250→115 total), then completed onboarding. Force-stop/relaunch retained 115 gold/70 spores/completed tutorial. Campaign showed stage 1 Cleared, stage 2 New and later stages Locked; stage-2 battle showed upgraded HP 385/144/210 and the preserved sparse formation. Pause and manual Orbi signature queue were visible in capture 28. Further acceptance checks are in progress under artifacts/device-5584.

### Step 4 guided upgrades — complete native functional acceptance

- Continued the same fresh-account run through all seven stages using Auto/Finish, only the three guaranteed starters and earned gold. Kira stayed level 1 and off the team. Upgraded all three starters once between stages, reaching level 9 before the boss; wallet before the boss was 290 gold. Cauldron Keeper was defeated with Thane down and Orbi/Luma surviving. The boss granted 600 gold, 30 spores and 11 XP; Next stage was disabled. Final wallet: **890 gold / 192 spores**. No debug grants, save edits or extra summons were used.
- Campaign displayed all seven stages Cleared. Replaying stage 1 displayed the practice/no-repeat-rewards result. A second force-stop/relaunch retained all clears, the final wallet, the level-9 starters and Front 2/Rear 2/Rear 4 formation. Phone views and a temporary 1600×1200 landscape viewport were readable; native touch scrolling exposed Kira's full passive and status-chance note in the workshop. The tablet-sized viewport is layout evidence, not a physical tablet test.
- Native evidence: artifacts/device-5584/46-boss-result.png, 51-boss-wallet.png, 53-practice-selected.png, 54-practice-result.png, 55-tablet-relaunch.png, 56-tablet-campaign.png, 57-tablet-team.png and 59-tablet-kit-scroll.png. Selected captures are preserved in docs/references/android-upgrade-step4.png, android-boss-step4.png and android-campaign-step4.png. Final logs: step4-game.log and step4-system-crash.log in the device artifact folder; no game exception/error was found in the captured Unity log.
- Limits/findings: the initial Android System UI ANR was an emulator startup issue; no physical-device stability, frame-rate or startup-time claim is made. Agent-operated Auto/Finish does not establish the 15–30 minute human-play target or first-time discoverability. An attempted Home tap behind the victory modal correctly had no effect; navigation through Team → Back worked. The initial local screenshot helper mishandled the adb -p flag; corrected the ignored helper to forward arguments and captured evidence successfully. Restored the emulator viewport and stopped the isolated read-only emulator after acceptance.
- Delivered this increment: compatible first-upgrade guidance, 9/9 passing PlayMode tests, reviewed captures, a new release-configuration APK and the complete native chapter/persistence/practice route. The prior 122/122 EditMode and 20-seed balance results apply to unchanged rules and were not rerun this increment. Updated PRODUCTION_PATH, SLICE_CONTENT, LEVEL_PROGRESSION, BUILD_GUIDE and test guidance. Step 4 remains active for human pacing/discoverability and bespoke content polish; later production progression, server authority and physical-device/store gates remain separate. No staging or commit performed.

## 2026-09-15 — GitHub checkpoint requested

- User explicitly requested committing and pushing all project additions. Reviewed the accumulated implementation, content, Unity settings/metas, build tools, tests, living documentation and reference images since af63cdc. The existing branch is main and the configured destination is origin at OneVillage83/FunGuy-s-WorkingName-; fetching confirmed no intervening remote changes before preparation.
- This checkpoint includes the production foundations through step 4 and their documented evidence. Generated .utmp native build-cache changes remain local and unstaged; ignored artifacts (including the APK and raw test/device logs) remain local. Existing tracked caches were not deleted or reset. Selected evidence images under docs/references are included, with PNG assets using the existing Git LFS rules.
- Source whitespace validation passed. This publication task changes no gameplay and does not rerun Unity suites; actual preceding validation remains 122/122 EditMode, 9/9 PlayMode, the release-configuration APK and the native chapter route detailed above. Human pacing/content polish and later production gates remain open. Publication uses a normal main push without rewriting remote history.
- Staging exposed trailing spaces in newly tracked Unity YAML/meta files and extra terminal blank lines in two C# files that the earlier tracked-only check did not inspect. Normalized only that whitespace and passed the complete staged check; no serialized values or code behavior changed. Git's initial broad staging command reported the ignored cache path, so staging was completed with an explicit source-file list and checked to contain no .utmp entries.

## 2026-09-21 — Unity toolchain clarification and v2 project release

- **Status:** Implemented repository-side version clarification; local Unity recovery on collaborator machines remains an environment task.
- **Affected files/systems:** `FunGuy's/ProjectSettings/ProjectSettings.asset`, `FunGuy's/ProjectSettings/ProjectVersion.txt` (verified unchanged), `BUILD_GUIDE.md`, root `VERSION.md`; package manifest/lockfile reviewed but not changed.
- **Changes/findings and rationale:** Repository history confirms `ProjectVersion.txt` has declared Unity `6000.3.2f1` since the initial project commit; the Codex production-foundation checkpoint and later Garrett roster integration did not change the editor version. Historical project logs also record successful compilation/tests and Android builds on that exact editor. The human-facing game/project release is now named **v2**; Unity `bundleVersion` is `2.0.0` and Android `versionCode` is `2`. Editor and game version numbers are now documented separately to prevent future confusion.
- **Production-path impact:** No gameplay architecture, content schema, package version, dependency, save schema, or Unity editor migration is introduced. The existing pinned-toolchain policy remains in force; editor upgrades must be isolated and validated separately.
- **Validation:** Verified the pinned editor file remains `6000.3.2f1`; verified only the intended PlayerSettings version fields are changed; package manifest/lockfile are untouched. Unity EditMode/PlayMode suites and Android packaging were not run for this metadata/documentation-only change. The latest Garrett-roster runtime still requires a fresh Unity validation pass after collaborator environment recovery.
- **Remaining work:** Garrett should open a clean generated cache with Unity 6000.3.2f1, run `git lfs pull`, and report the first compiler errors if Safe Mode persists. Then rerun the repository EditMode/PlayMode suites against the post-roster source before claiming the current roster merge as runtime-validated.

## 2026-09-21 — Garrett roster battle restoration and campaign opponent migration

- **Status:** Implemented in source; Unity runtime validation pending.
- **Affected files/systems:** `characters.json`, `stages.json`, `skills.json`, campaign/content validation and conversion code, battle preview/smoke checks, placeholder portrait selection, EditMode fixtures, `SLICE_CONTENT.md`, `BUILD_GUIDE.md`, `PRODUCTION_PATH.md`.
- **Changes/findings and rationale:** Garrett confirmed the synced project opens normally on the pinned editor but battles still depend on the retired placeholder content. The existing `FungusPortrait` is already a code-native universal placeholder, so no per-character sprite asset is required. All 71 roster fighters now receive a stable placeholder variation and biome color (including Cosmic). The bundled legacy enemy catalog was removed, orphaned placeholder enemy skills were removed, and all seven campaign stages now reference real Garrett roster character IDs. Stage IDs/rewards/unlock order remain stable for save compatibility.
- **Production-path impact:** Campaign opponents and collectible fighters now share one authored character source and one stat conversion path, reducing duplicate placeholder content. The serialized `WaveUnit.enemyId` name remains temporarily for stage-schema compatibility but semantically references a roster character in `garrett-roster-campaign-v2`. `EnemyDef`/the empty `GameData.Enemies` surface remains only as compatibility scaffolding and can be removed in a later schema cleanup after all tooling/fixtures migrate.
- **Validation:** Repository-level static validation passed: 71 unique roster IDs, 0 bundled legacy enemies, 14 waves, 22 distinct real-roster opponent IDs, all character skill references resolved, every stage opponent exists, explicit wave slots are unique, and no retired placeholder enemy IDs/skills remain in bundled runtime JSON. Unity EditMode/PlayMode tests and Android packaging have **not** been run in this environment. Historical 2026-09-15 122/122 EditMode, 9/9 PlayMode and Android evidence predates the Garrett roster/campaign migration and is not current validation.
- **Remaining work:** Garrett should clean generated Unity caches, open with Unity 6000.3.2f1, run the full automated suites and report the first compiler error if Safe Mode appears. After the game boots, rerun campaign balance because previous tuning used different opponents/kits. Bespoke 71-character skills and final sprites remain separate content work.

## 2026-09-22 — Garrett Unity-authored catalog repair

- **Status:** Source repair implemented on review branch; fresh Unity validation pending on Garrett's machine.
- **Affected files/systems:** Unity-authored content loader/asset/editor menu, GameData normalization/finalization, UnityContentCatalog.asset, new EditMode regression tests, package/tooling cleanup, ignore rules, UNITY_AUTHORING.md and GARRETT_UNITY_AUTHORING_REPAIR.md.
- **Changes/findings and rationale:** Garrett's WIP EditMode report ran 122 tests (94 passed / 28 failed); every failing case ultimately reported the same `Stage s_1_1: missing waves` content-load error. The committed ScriptableObject proved runtime aliasing: Stage 1 had been persisted with `waves: []` and gold `99999`, exactly matching an intentional runtime mutation in CampaignSessionTests. The loader now deep-clones authored DTOs before validation/runtime use, schema validation is non-mutating, Stage 1 is repaired, and production GameData normalization removed by the WIP refactor is restored. Five unrelated package upgrades and IDE/test/TMP side effects were reverted/removed.
- **Production-path impact:** Preserves the Inspector-authoring experiment while restoring the intended source/runtime ownership boundary: authored assets are immutable input; mutable runtime catalogs are detached snapshots. JSON remains seed/fallback only when the asset is absent. No package/editor migration is part of this feature.
- **Validation:** Existing WIP XML was analyzed and tied to the asset-aliasing defect. Added focused EditMode regression coverage for asset integrity/deep-copy behavior. Unity tests cannot be executed in this environment, so no passing suite is claimed. Package pins and source diff were statically reviewed.
- **Remaining work:** Merge the cross-fork repair into Garrett's feature branch, clean generated Unity caches, validate the catalog, then run full EditMode and PlayMode suites under Unity 6000.3.2f1. If failures remain, review only the first fresh error before further changes.

## 2026-09-22 — Unity-authored battle energy symptom traced to blank effect targets

- **Status:** Source fix implemented on Garrett review branch; fresh Unity validation pending.
- **Affected files/systems:** `GameData.cs` runtime effect normalization; `BattleSim.cs` and `BattleEffects.cs` optional target/stat fallback semantics; BattleSession and UnityContentCatalog EditMode regression tests; Garrett repair notes.
- **Changes/findings and rationale:** Garrett reported Battle stopping around energy generation after the prior catalog repair. Source tracing showed start-turn +20 energy executes immediately before skill resolution. Unity ScriptableObjects serialize unset optional `EffectDef.target`/`stat` strings as `""`, while JSON omission produces `null`. The battle engine used null-only `??` fallbacks, so an imported basic effect with blank target attempted `SelectTargets("")` after the energy event and threw instead of falling back to the skill's `EnemyFront` target. Runtime content now canonicalizes blank optional effect strings to null, and the domain engine independently treats null/blank identically.
- **Production-path impact:** This is an authoring-adapter compatibility fix, not a change to energy rules. Existing energy amounts/timing remain 20 start-turn, 5 damaging-hit attacker, 10 damage-taken recipient, and 15 kill replacement. JSON and Unity-authored content now share one optional-field semantic contract.
- **Validation:** Added a focused domain regression that reproduces blank Unity strings and verifies +20 energy, skill execution, damage, +5 hit energy and continued Running state. Added a catalog regression proving the detached runtime copy normalizes blank target without mutating the authored asset. Unity cannot be run from this environment; Garrett must rerun the fresh suites locally.
- **Remaining work:** Garrett should fast-forward his feature branch to the updated review commit, clean generated caches if needed, run catalog validation, then EditMode and PlayMode suites. If Battle still stops, capture the first fresh Console exception and stack trace.

## 2026-09-22 — Garrett learning/debugging log and reusable checklist

- **Status:** Documentation added on the Garrett review branch; no gameplay behavior changed.
- **Affected files/systems:** Added `GARRETT_DEBUG_LOG_AND_CHECKLIST.md`; linked it from `UNITY_AUTHORING.md` and `GARRETT_UNITY_AUTHORING_REPAIR.md`.
- **Changes/findings and rationale:** Added a beginner-oriented chronological record of the Unity version/cache issue, 71-character campaign migration, ScriptableObject aliasing defect, GameData normalization regression, package drift, generated IDE/test artifacts, and the blank Unity effect-target bug that made Battle appear to fail during energy generation. Each entry distinguishes observed symptom, root cause, repair, validation and lesson. Added pre-test/runtime/post-test checklists, exact energy rules, Git workflow, first-error debugging order, and a reusable error-capture template so Garrett can practice systematic diagnosis rather than chasing cascading errors.
- **Production-path impact:** Documentation only. Reinforces the existing requirement that authored content, runtime state, dependency changes and generated files remain separate concerns and that validation evidence is recorded rather than assumed.
- **Validation:** Reviewed the checklist against the current branch history, repair commits and documented battle/content rules. No Unity run was required for this documentation-only addition.
- **Remaining work:** Garrett should use the checklist during his next Unity 6000.3.2f1 validation pass and append new G-XXX entries as future issues are reproduced and resolved.

## 2026-09-23 — Garrett working branch review, merge and Unity meta cleanup

- **Status:** Garrett's PR #6 reviewed and merged into upstream `main`; follow-up Unity metadata cleanup prepared immediately after merge.
- **Affected files/systems:** PR #6 Unity-authored content workflow and battle fixes; `Assets/Resources/PerformanceTestRunInfo.json.meta`; `Assets/Resources/PerformanceTestRunSettings.json.meta`; project history only otherwise.
- **Changes/findings and rationale:** Garrett's fork `main` was six commits ahead and zero behind upstream and GitHub reported the PR mergeable. Review confirmed the Unity-authored `UnityContentCatalog.asset`, Inspector authoring/validation menu, detached runtime catalog loading, blank optional effect target/stat compatibility fix, pinned Unity 6000.3.2f1 toolchain and pinned package manifest were all preserved. No Visual Studio cache or TestResults XML was present in the PR. Review also found the two tracked performance-test resource `.meta` files had been dropped while new GameData metas were added; their GUIDs are not referenced elsewhere, so this was not a gameplay blocker, but restoring them avoids Unity regenerating new GUIDs locally and keeps the tracked asset set stable.
- **Production-path impact:** The Unity Inspector stat/content editing path is now part of upstream `main` without changing the existing domain/application ownership boundaries. Restoring the two metas is repository hygiene only and does not alter gameplay, content balance or runtime rules.
- **Validation:** PR diff and branch ancestry reviewed; Unity version and package manifest checked against upstream pins; old performance GUIDs searched and found unreferenced. No new Unity EditMode/PlayMode execution was performed in this environment, so Garrett's local runtime validation remains the execution gate.
- **Remaining work:** Garrett should pull the merged `main`, validate the Unity catalog, run fresh EditMode/PlayMode suites and continue art work only after confirming the merged branch behaves as expected. The duplicate-to-evolution feature and biome filters remain separate follow-up work.
